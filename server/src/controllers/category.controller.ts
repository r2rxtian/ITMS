import type { Request, Response } from 'express';
import * as repository from '../repositories/category.repository.js';
import { writeAudit } from '../repositories/audit.repository.js';
import { ApiError } from '../utils/api-error.js';
export async function list(request:Request,response:Response){const data=await repository.listCategories(request.query.includeArchived==='true');response.json({success:true,message:'Categories retrieved.',data});}
export async function create(request:Request,response:Response){const data=await repository.createCategory(request.body.name,request.body.description??null);await writeAudit({userId:request.session.user!.id,action:'CATEGORY_CREATED',entityType:'CATEGORY',entityId:data.id,description:`Created category ${data.name}.`,ipAddress:request.ip});response.status(201).json({success:true,message:'Category created successfully.',data});}
export async function update(request:Request,response:Response){const data=await repository.updateCategory(Number(request.params.id),request.body);if(!data)throw new ApiError(404,'Category not found.');await writeAudit({userId:request.session.user!.id,action:'CATEGORY_UPDATED',entityType:'CATEGORY',entityId:data.id,description:`Updated category ${data.name}.`,ipAddress:request.ip});response.json({success:true,message:'Category updated successfully.',data});}
export async function archive(request:Request,response:Response){const data=await repository.archiveCategory(Number(request.params.id));if(!data)throw new ApiError(404,'Category not found.');await writeAudit({userId:request.session.user!.id,action:'CATEGORY_ARCHIVED',entityType:'CATEGORY',entityId:data.id,description:'Archived category.',ipAddress:request.ip});response.json({success:true,message:'Category archived successfully.',data});}
